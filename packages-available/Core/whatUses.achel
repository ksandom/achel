# What uses a particular feature? --whatUses=featureName . Note that featureName does not include the "--" that is used on the command line. ~ debug,language
parameters regex

loadAllMacros
getCategory Macros
recursiveRefine ~!Local,regex!~
templateOut whatUses
