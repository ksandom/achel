# Get the possible repo path for the current application. Right now this is speculative because this data isn't currently stored. It can be guessed and be right most of the time by taking the programName, but there is no requrement that the programName and the repoName be the same. ~ bug,hidden

debug 0,asd 1
set Local,possibleRepoPath,~!General,configDir!~/repos/~!General,programName!~
debug 0,asd 2
get Local,possibleRepoPath
debug 0,asd 3
singleString
debug 0,asd 4
verbose 1