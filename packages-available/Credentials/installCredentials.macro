# Install Credentials related stuff ~ install,credentials
#onDefine registerForEvent Install,general,installCredentials

debug 1,Creating credentials defaults
setIfNotSet Credentials,defaultKey,id_rsa 
saveStoreToConfig Credentials
