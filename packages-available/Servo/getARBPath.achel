# Get the path to Achel Reality Bridge external dependancies. --getARBPath=Category,variable ~ lmtracker,json,hidden
parameters Category,variable

setNested ~!Local,Category!~,~!Local,variable!~,~!General,configDir!~/repos/achel/packages-available/Servo
makeLocalAvailable ~!Local,variable!~
