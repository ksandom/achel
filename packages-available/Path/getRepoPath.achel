# Get the path the a particular repository. --getRepoPath=Category,variable,repoName ~ path,repository
parameters category,variable,repoName

set ~!Local,category!~,~!Local,variable!~,~!General,configDir!~/repos/~!Local,repoName!~

makeAvailable ~!Local,category!~,~!Local,variable!~
