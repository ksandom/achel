# Create a faucet that only keeps the last value on every channel. --createLastFaucet=faucetName ~ faucet,last,Last,manipulations
parameters faucetName

createMappedCallFaucet ~!Local,faucetName!~,lastOfEach
