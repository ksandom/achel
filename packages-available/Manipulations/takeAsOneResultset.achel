# Take a single key from every result and make it one result set. --takeAsOneResultset=key where key is a key in each result in the resultSet. ~ Manipulator

take ~!Global,takeAsOneResultset!~
flatten
finalFlatten
