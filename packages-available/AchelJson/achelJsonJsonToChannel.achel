# AchelJson. Takes json in the default channel, and puts its contents into the appropriate channels. ~ hidden,achel,json,worker

stashResults Local,jsonChannels
retrieveResults Local,jsonChannels,default

loop
	set ["Local","interpreted",~!Result,line!~]
	retrieveResults Local,interpreted
	loop Item,
		setNested Local,recompiled,~!Item,key!~,0,~!Item,line!~


retrieveResults Local,recompiled

# nested
# outNow

