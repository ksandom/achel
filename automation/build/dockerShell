#!/bin/bash

tag=`./automation/build/tag`

appName=`basename $0`

if [ "$appName" == 'wrapper' ]; then
  echo "This is not intended to be run directly." >&2
  exit 1
fi

docker run -it  --env COMMAND=bash kjsandom/achel:$tag /usr/installs/achel/automation/dockerInternal/internalWrapper "$@"

